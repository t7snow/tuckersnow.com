---
import { getCollection } from 'astro:content';
import BookCard from './BookCard.astro';

// Fetch and sort blog posts
const bookposts = await getCollection('bookposts');
 // Display only the 3 most recent posts
---
<section class="">
  <h1 class="mb-4 text-5xl font-bold tracking-tight text-black ">📚 Book Notes</h2>
    <p class="mb-4">
      Starting in 2025, I looked to take longer notes on the books I have been reading. These are hopefully useful in some fashion to you.
    </p>
  <div class="my-12 grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4 mx-auto w-full">
    {bookposts.map((post) => (
      <BookCard post={post} />
    ))}
  </div>
   
</section>